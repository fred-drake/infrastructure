apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: paperless-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/fred-drake/infrastructure.git
    targetRevision: HEAD
    path: cluster/apps/paperless/paperless
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
