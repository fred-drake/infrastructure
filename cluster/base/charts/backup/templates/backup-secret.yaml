apiVersion: v1
kind: Secret
metadata:
    name: backup-secret
    namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
    resticPassword: {{ .Values.restic.password }}
    s3User: {{ .Values.s3.user }}
    s3Password: {{ .Values.s3.password }}
