{{- if .Values.storageNodes -}}
{{- range .Values.storageNodes }}
---
apiVersion: v1
kind: Node
metadata:
  name: {{ . }}
  annotations:
    persistent-storage: 'true'
spec:
  taints:
    - effect: NoSchedule
      key: persistent-storage
{{ end }}
{{- end -}}
