apiVersion: v1
kind: Secret
metadata:
    name: minio-secret
    namespace: longhorn-system
type: Opaque
stringData:
    AWS_ACCESS_KEY_ID: {{ .Values.backup.awsAccessKeyId }}
    AWS_SECRET_ACCESS_KEY: {{ .Values.backup.awsSecretAccessKey }}
    AWS_ENDPOINTS: {{ .Values.backup.awsEndpoints }}
