apiVersion: v1
kind: Secret
metadata:
    name: minio-secret
    namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
    user: {{ .Values.minio.user }}
    password: {{ .Values.minio.password }}
