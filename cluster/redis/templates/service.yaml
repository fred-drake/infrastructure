apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name | quote }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.name | quote }}
spec:
  type: NodePort
  ports:
  - name: redis
    port: 6379
    targetPort: redis
  selector:
    app: redis
