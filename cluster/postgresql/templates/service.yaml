apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name | quote }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Values.name | quote }}
  type: NodePort
  ports:
    - port: 5432
      targetPort: postgres
      nodePort: 30177
