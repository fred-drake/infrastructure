{{- if .Values.storageNodes -}}
{{- range .Values.storageNodes }}
---
apiVersion: v1
kind: Node
metadata:
  name: {{ . }}
spec:
  taints:
    - effect: NoExecute
      key: storage
      value: longhorn
{{ end }}
{{- end -}}
