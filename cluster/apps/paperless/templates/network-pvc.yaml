---
apiVersion: v1
kind: PersistentVolume
metadata:
    name: paperless-scanner
    annotations:
        velero.io/exclude-from-backup: "true"
spec:
    storageClassName: ""
    capacity:
        storage: 1Gi
    accessModes:
        - ReadWriteMany
    persistentVolumeReclaimPolicy: Retain
    mountOptions:
        - hard
    nfs:
        path: /mnt/user/ScannerIncoming
        server: "larussa-50.{{ .Values.domain }}"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: paperless-scanner
    namespace: paperless
spec:
    storageClassName: ""
    volumeName: paperless-scanner
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 1Gi
