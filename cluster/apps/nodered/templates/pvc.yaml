apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nodered-config
  namespace: {{ .Release.Namespace }}
spec:
  storageClassName: longhorn
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
