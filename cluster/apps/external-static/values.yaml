
image:
  repository: ghcr.io/linuxserver/nginx
  tag: 1.20.2

backup:
  claims:
    - name: nginx-content
      subPath: content
    - name: nginx-config
      subPath: config
  affinity:
    key: app
    values:
      - nginx
