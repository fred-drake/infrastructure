kind: Service
apiVersion: v1
metadata:
    name: {{ .Release.Name }}
    namespace: {{ .Release.Namespace }}
spec:
    type: LoadBalancer
    loadBalancerIP: {{ .Values.loadBalancerIP }}
    selector:
        app: mqtt
    ports:
        - port: 1883
          targetPort: 1883
          name: mqtt
