apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-config
  namespace: prowlarr
spec:
  storageClassName: gfs
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
