apiVersion: v1
kind: Secret
metadata:
    name: netbox-secret
    namespace: netbox
type: Opaque
stringData:
    superUserEmail: {{ .Values.netbox.superUserEmail }}
    superUserPassword: {{ .Values.netbox.superUserPassword }}
    allowedHost: {{ .Values.netbox.allowedHost }}
