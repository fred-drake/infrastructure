apiVersion: v1
kind: Secret
metadata:
    name: postgres-secret
    namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
    postgresUser: {{ .Values.postgres.postgresUser }}
    superUserPassword: {{ .Values.postgres.superUserPassword }}
    replicationUserPassword: {{ .Values.postgres.replicationUserPassword }}
