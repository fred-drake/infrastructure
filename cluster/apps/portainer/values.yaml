portainer:
  image:
    repository: portainer/portainer-ce
    tag: 2.9.3
  resource:
    requests:
      cpu: 100m
      memory: 128Mi
  service:
    type: ClusterIP
  persistence:
    enabled: true
    existingClaim: portainer-config
