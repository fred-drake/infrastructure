apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: portainer-config
  namespace: {{ .Release.Namespace }}
spec:
  storageClassName: longhorn
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
