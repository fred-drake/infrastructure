apiVersion: v1
kind: ConfigMap
metadata:
  name: environment-map
  namespace: {{ .Release.Namespace }}
data:
  runtime-config.js: |
    window['runConfig'] = {
        nodeRedUrlPrefix: '{{ .Values.nodeRedUrlPrefix }}',
        scannerUrlPrefix: '{{ .Values.scannerUrlPrefix }}'
    }
