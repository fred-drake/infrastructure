kind: Service
apiVersion: v1
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  type: LoadBalancer
  selector:
    app: {{ .Release.Name }}
  ports:
    - port: 80
      targetPort: 3000
      name: web
  loadBalancerIP: {{ .Values.loadBalancerIP }}
