apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-config
  namespace: grafana
spec:
  storageClassName: rook-ceph-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
