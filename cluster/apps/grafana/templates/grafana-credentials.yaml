apiVersion: v1
kind: Secret
metadata:
    name: grafana-credentials
    namespace: grafana
type: Opaque
stringData:
    admin-user: {{ .Values.admin.adminUser }}
    admin-password: {{ .Values.grafana.admin.adminPassword }}
