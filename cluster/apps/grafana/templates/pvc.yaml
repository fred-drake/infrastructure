apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-config
  namespace: grafana
spec:
  storageClassName: longhorn
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
