readarr:
    ingress:
        main:
            hosts:
                - host: readarr.domain.com
                  paths:
                    - path: /
                      pathType: Prefix
            tls:
                - hosts:
                    - readarr.domain.com
                  secretName: readarr-tls
