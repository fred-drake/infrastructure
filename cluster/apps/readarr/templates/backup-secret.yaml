apiVersion: v1
kind: Secret
metadata:
    name: backup-secret
    namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
    resticPassword: {{ .Values.backup.restic.password }}
    s3User: {{ .Values.backup.s3.user }}
    s3Password: {{ .Values.backup.s3.password }}
