apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gaps-config
  namespace: gaps
spec:
  storageClassName: longhorn
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
