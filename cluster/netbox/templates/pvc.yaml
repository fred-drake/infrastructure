kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ required "pvc name required!" .Values.existingClaim }}
spec:
  storageClassName: kadalu.storage-pool-1
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ required "pvc storage required!" .Values.claimSize }}

