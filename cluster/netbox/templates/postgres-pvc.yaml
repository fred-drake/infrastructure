kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ required "pvc name required!" .Values.postgresql.existingClaim }}
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ required "pvc storage required!" .Values.postgresClaimSize }}
