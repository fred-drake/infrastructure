postgresql:
  image:
    tag: "12"
  username: postgres
  secretName: postgresql-credentials
  database: netbox
  existingClaim: netbox-postgresql-data
postgresql-pvc:
  name: netbox-postgresql-data
  storage: 10Gi
