---
- name: create application container
  community.docker.docker_container:
    name: "{{ role_name }}"
    image: "influxdb:2.0"
    pull: "{{ container_pull }}"
    hostname: "{{ role_name }}"
    env:
      TZ: "{{ default_timezone }}"
      DOCKER_INFLUXDB_INIT_MODE: setup
      DOCKER_INFLUXDB_INIT_USERNAME: "{{ services.influxdb_wifi.docker_init_username }}"
      DOCKER_INFLUXDB_INIT_PASSWORD: "{{ services.influxdb_wifi.docker_init_password }}"
      DOCKER_INFLUXDB_INIT_ORG: "{{ services.influxdb_wifi.docker_init_org }}"
      DOCKER_INFLUXDB_INIT_BUCKET: "{{ services.influxdb_wifi.docker_init_bucket }}"
      INFLUXD_HTTP_BIND_ADDRESS: "{{ services.influxdb_wifi.influxd_http_bind_address }}"
      INFLUXD_TLS_CERT: "{{ services.influxdb_wifi.influxd_tls_cert }}"
      INFLUXD_TLS_KEY: "{{ services.influxdb_wifi.influxd_tls_key }}"
    volumes:
      - "{{ appdata_dir }}/influxdb-wifi/data:/var/lib/influxdb2"
      - "{{ appdata_dir }}/influxdb-wifi/config:/etc/influxdb2"
    container_default_behavior: "{{ container_default_behavior }}"
    network_mode: "{{ container_network }}"
    restart_policy: unless-stopped
    networks:
      - name: "{{ container_network }}"
        ipv4_address: "{{ services.influxdb_wifi.ip }}"
    networks_cli_compatible: "{{ container_networks_cli_compatible }}"
    state: "{{ container_state }}"
  notify:
    - deep prune docker
  tags:
    - influxdb_wifi
    - docker_update
