gateway_208: "{{ secret.network.admin.gateway }}"
netmask_208: "{{ secret.network.admin.cidr }}"

gateway_50: "{{ secret.network.services.gateway }}"
netmask_50: "{{ secret.network.services.cidr }}"

gateway_40: "{{ secret.network.iot.gateway }}"
netmask_40: "{{ secret.network.iot.cidr }}"

gateway_30: "{{ secret.network.workstation.gateway }}"
netmask_30: "{{ secret.network.workstation.cidr }}"

bridge_network: br0
