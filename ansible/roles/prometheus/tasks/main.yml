- name: Prometheus config
  ansible.builtin.template:
    src: prometheus.yml.j2
    dest: "{{ glusterfs_appdata_dir }}/prometheus/prometheus/config/prometheus.yml"
    mode: "0644"
  notify:
    - Restart prometheus
