apiVersion: v1
kind: Secret
metadata:
  name: helm-secrets
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
stringData:
  whoogle.yaml: |
    ingress:
      main:
        # hosts:
        #   - host: "whoogle.{{ secret.domain }}"
        #     paths:
        #       - path: /
        #         pathType: Prefix
        tls:
          - hosts:
              - "whoogle.{{ secret.domain }}"
            secretName: whoogle-tls
