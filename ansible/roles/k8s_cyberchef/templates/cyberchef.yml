---
apiVersion: v1
kind: Namespace
metadata:
  name: "{{ services.cyberchef.name }}"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ services.cyberchef.name }}"
  namespace: "{{ services.cyberchef.name }}"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "{{ services.cyberchef.name }}"
  template:
    metadata:
      labels:
        app: "{{ services.cyberchef.name }}"
    spec:
      containers:
        - name: "{{ services.cyberchef.name }}"
          image: mpepping/cyberchef
          imagePullPolicy: Always
          env:
            - name: TZ
              value: "{{ default_timezone }}"
---
kind: Service
apiVersion: v1
metadata:
  name: "{{ services.cyberchef.name }}"
  namespace: "{{ services.cyberchef.name }}"
spec:
  type: ClusterIP
  selector:
    app: "{{ services.cyberchef.name }}"
  ports:
    - port: 80
      targetPort: 8000
      name: ui-port
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{ services.cyberchef.name }}"
  namespace: "{{ services.cyberchef.name }}"
spec:
  rules:
  - host: "{{ services.cyberchef.host }}.{{ domain_name }}"
    http: 
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: "{{ services.cyberchef.name }}"
            port:
              number: 80
---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: redirect-no-domain
  namespace: "{{ services.cyberchef.name }}"
spec:
  redirectRegex:
    regex: "^http://{{ services.cyberchef.name }}/.*"
    replacement: "https://{{ services.cyberchef.name }}.{{ domain_name }}/"
    permanent: true
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: "{{ services.cyberchef.name }}"
  namespace: "{{ services.cyberchef.name }}"
entryPoints:
    - web
    - websecure
spec:
  routes:
    - match: Host(`{{ services.cyberchef.name }}`)
      kind: Rule
      services:
        - name: api@internal
          kind: TraefikService
      middlewares:
      - name: redirect-no-domain
