- name: Install necessary apt libraries
  ansible.builtin.apt:
    name:
      - python3
      - python3-requests
      - python3-proxmoxer
    update_cache: true

# - name: Install necessary libraries
#   ansible.builtin.pip:
#     name:
#       - requests
#       - proxmoxer
