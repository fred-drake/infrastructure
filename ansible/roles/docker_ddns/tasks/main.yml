---
- name: create application container
  community.docker.docker_container:
    name: "{{ role_name }}"
    image: "ghcr.io/akarys42/cloudflare-ddns-docker"
    pull: "{{ container_pull }}"
    hostname: "{{ role_name }}"
    env:
      TZ: "{{ default_timezone }}"
      CF_DDNS_TOKEN: "{{ services.ddns.token }}"
      CF_DDNS_DOMAINS: "{{ services.ddns.domains }}"
    container_default_behavior: "{{ container_default_behavior }}"
    network_mode: "{{ container_network }}"
    restart_policy: "{{ container_restart_policy }}"
    networks:
      - name: "{{ container_network }}"
        ipv4_address: "{{ services.ddns.ip }}"
    networks_cli_compatible: "{{ container_networks_cli_compatible }}"
    state: "{{ container_state }}"
  notify:
    - deep prune docker
  tags:
    - ddns
    - docker_update
