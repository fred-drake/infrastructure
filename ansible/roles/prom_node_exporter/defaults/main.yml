exporter_binary_version: "1.3.1"

# Maps the output from ansible_architecture to what you'd use for downloading
arch_map:
  armv7l: armv7
  aarch64: arm64
  x86_64: amd64

prefix: https://github.com/prometheus/node_exporter/releases/download/v
