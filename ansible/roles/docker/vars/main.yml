gateway_208: "{{ network.admin.gateway }}"
netmask_208: "{{ network.admin.cidr }}"

gateway_50: "{{ network.services.gateway }}"
netmask_50: "{{ network.services.cidr }}"

gateway_40: "{{ network.iot.gateway }}"
netmask_40: "{{ network.iot.cidr }}"

gateway_30: "{{ network.workstation.gateway }}"
netmask_30: "{{ network.workstation.cidr }}"

bridge_network: br0
