---
- name: create scanner incoming volume
  block:
    - &volume
      name: create volume
      community.docker.docker_volume:
        volume_name: epson_scanner_outgoing
        driver: local
        driver_options:
          type: cifs
          device: "//{{ cifs_server }}/ScannerIncoming"
          o: "{{ cifs_docker_mount_options }},gid=200,uid=200"
        recreate: options-changed
  rescue:
    - name: remove application container
      community.docker.docker_container:
        name: "epson_scanner"
        state: absent
        container_default_behavior: compatibility
    - <<: *volume
  tags:
    - epson_scanner

- name: create application container
  community.docker.docker_container:
    name: "epson_scanner"
    image: "registry.{{ secret.domain }}/epson-scanner"
    pull: "{{ container_pull }}"
    hostname: "epson_scanner"
    env:
      TZ: "{{ default_timezone }}"
    mounts:
      - source: epson_scanner_outgoing
        target: /outgoing
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    container_default_behavior: "{{ container_default_behavior }}"
    network_mode: "{{ secret.docker.network.container }}"
    restart_policy: unless-stopped
    networks:
      - name: "{{ secret.docker.network.container }}"
        ipv4_address: "{{ secret.services.epson_scanner.ip }}"
    networks_cli_compatible: "{{ container_networks_cli_compatible }}"
    state: "{{ container_state }}"
    privileged: true
  notify:
    - deep prune docker
  tags:
    - epson_scanner
    - docker_update
