---
- name: install microk8s snap
  snap:
    name: microk8s
    classic: true
    state: present

- name: add user to microk8s group
  user:
    name: "{{ ansible_user }}"
    groups: microk8s
    append: true

- name: install pip requirements
  pip:
    name: "{{ item }}"
  loop:
    - openshift
