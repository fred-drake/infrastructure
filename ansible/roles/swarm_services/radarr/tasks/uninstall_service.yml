- name: Remove service
  community.docker.docker_swarm_service:
    name: radarr
    state: absent

- name: Remove backup service
  community.docker.docker_swarm_service:
    name: backup-radarr
    state: absent

- name: Remove volume videos
  community.docker.docker_volume:
    name: radarr_videos
    state: absent

- name: Remove volume movies
  community.docker.docker_volume:
    name: radarr_movies
    state: absent
