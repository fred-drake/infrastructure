- name: Create ArgoCD namespace
  kubernetes.core.k8s:
    api_version: v1
    kind: Namespace
    name: argocd
    state: present

- name: Apply ArgoCD
  kubernetes.core.k8s:
    namespace: argocd
    state: present
    src: "argocd_install.yml"
