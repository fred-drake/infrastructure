---
apiVersion: v1
kind: Namespace
metadata:
  name: "{{ services.gaps.name }}"
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: "{{ services.gaps.name }}"
  namespace: "{{ services.gaps.name }}"
spec:
  serviceName: "{{ services.gaps.name }}"
  replicas: 1
  selector:
    matchLabels:
      app: "{{ services.gaps.name }}"
  volumeClaimTemplates:
  - metadata:
      name: conf
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 20Gi
  template:
    metadata:
      labels:
        app: "{{ services.gaps.name }}"
    spec:
      containers:
      - name: "{{ services.gaps.name }}"
        image: housewrecker/gaps:arm-latest
        env:
        - name: TZ
          value: "{{ default_timezone }}"
        - name: ENABLE_SSL
          value: "false"
        - name: ENABLE_LOGIN
          value: "false"
        - name: BASE_URL
          value: "/"
        volumeMounts:
        - name: conf
          mountPath: /usr/data
---
kind: Service
apiVersion: v1
metadata:
  name: "{{ services.gaps.name }}"
  namespace: "{{ services.gaps.name }}"
spec:
  type: NodePort
  selector:
    app: "{{ services.gaps.name }}"
  ports:
    - port: 80
      targetPort: 8484
      name: ui-port
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{ services.gaps.name }}"
  namespace: "{{ services.gaps.name }}"
spec:
  rules:
  - host: "{{ services.gaps.name }}.{{ domain_name }}"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: "{{ services.gaps.name }}"
            port:
              number: 80
