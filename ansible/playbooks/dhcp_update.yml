- name: Update DHCP service
  hosts: critical_docker_services
  gather_facts: false
  become: true
  roles:
    - docker/base
    - role: docker/dhcp
      vars:
        container_pull: false
  any_errors_fatal: true
