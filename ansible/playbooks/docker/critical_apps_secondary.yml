- hosts: critical_docker_secondary
  gather_facts: false
  become: true
  roles:
    - docker/base
    - role: docker/dhcp
      vars:
        dhcp_id: dhcp2
    - role: docker/pihole
      vars:
        pihole_id: pihole2
    - docker/backup_client
