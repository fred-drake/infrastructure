# - import_playbook: docker/delete_containers.yml

# - import_playbook: kubernetes/nuke_cluster.yml

- hosts: scoreboard
  become: true
  gather_facts: false
  roles:
    - scoreboard/delete
  tags:
    - nuke
    - never
