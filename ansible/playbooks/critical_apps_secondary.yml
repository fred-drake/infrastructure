- hosts: docker1
  gather_facts: false
  become: true
  roles:
    - docker/base
    - role: docker/dhcp
      vars:
        dhcp_id: dhcp2

- hosts: docker5
  gather_facts: false
  become: true
  roles:
    - docker/base
    - role: docker/pihole
      vars:
        pihole_id: pihole2
