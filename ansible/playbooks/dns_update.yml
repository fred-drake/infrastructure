- hosts: critical_docker_primary
  gather_facts: false
  become: true
  roles:
    - docker/base
    - role: docker/pihole
      vars:
        pihole_id: pihole1
        container_pull: false

- hosts: critical_docker_secondary
  gather_facts: false
  become: true
  roles:
    - docker/base
    - role: docker/pihole
      vars:
        pihole_id: pihole2
        container_pull: false
